<div class="card">
  <div class="card-header">
     <h5>Carrito de compras</h1>
  </div>
  <div class="card-body">
    <table>
      <%= render(cart.line_items) %>
      <tfoot>
        <tr>
          <th colspan="2">Total:</th>
          <td class="price">
            <%= number_to_currency(cart.total_price) %>
          </td>
        </tr>
      </tfoot>
</table>
  </div>
  <div class="card-footer">
    <%= button_to 'Vaciar' , cart, method: :delete, data: { confirm: 'Estás seguro?' }, class: 'btn btn-primary' %>
  </div>
</div>
